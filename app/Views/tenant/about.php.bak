<?php
/**
 * Tenant About Page
 * Organization story, mission, and team information.
 */
$orgName = htmlspecialchars($org['name'] ?? 'Sports Club');
$tagline = htmlspecialchars($branding['tagline'] ?? '');
?>

<!-- Hero -->
<section class="bg-gradient-to-br from-surface-900 to-surface-800 text-white py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="max-w-3xl">
            <h1 class="text-3xl sm:text-4xl font-extrabold">About <?= $orgName ?></h1>
            <?php if ($tagline): ?>
            <p class="mt-4 text-xl text-surface-300"><?= $tagline ?></p>
            <?php endif; ?>
        </div>
    </div>
</section>

<!-- Story -->
<section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="lg:grid lg:grid-cols-2 lg:gap-16 items-center">
            <div>
                <h2 class="text-2xl sm:text-3xl font-extrabold text-surface-900">Our Story</h2>
                <div class="mt-6 space-y-4 text-surface-600 leading-relaxed">
                    <p><?= $orgName ?> is dedicated to providing a premier sports experience for players of all skill levels. Whether you're just picking up a paddle for the first time or competing at a high level, our facilities and programs are designed to help you improve, connect, and have fun.</p>
                    <p>Our mission is to build a vibrant community around the sports we love. We believe that great facilities, quality instruction, and a welcoming atmosphere make all the difference.</p>
                </div>
            </div>
            <div class="mt-10 lg:mt-0">
                <div class="rounded-2xl bg-gradient-to-br from-primary-100 to-primary-50 h-80 flex items-center justify-center">
                    <?php if (!empty($branding['hero_image'])): ?>
                    <img src="<?= htmlspecialchars($branding['hero_image']) ?>" alt="<?= $orgName ?>" class="w-full h-full object-cover rounded-2xl">
                    <?php else: ?>
                    <svg class="w-20 h-20 text-primary-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Values -->
<section class="py-20 bg-surface-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-2xl sm:text-3xl font-extrabold text-surface-900 text-center mb-12">What We Stand For</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="bg-white rounded-2xl border border-surface-100 p-8 text-center">
                <div class="w-14 h-14 rounded-2xl bg-primary-100 flex items-center justify-center mx-auto mb-4">
                    <svg class="w-7 h-7 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                </div>
                <h3 class="text-lg font-bold text-surface-800">Community First</h3>
                <p class="mt-2 text-sm text-surface-500">We foster an inclusive environment where everyone feels welcome, from beginners to pros.</p>
            </div>
            <div class="bg-white rounded-2xl border border-surface-100 p-8 text-center">
                <div class="w-14 h-14 rounded-2xl bg-primary-100 flex items-center justify-center mx-auto mb-4">
                    <svg class="w-7 h-7 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                </div>
                <h3 class="text-lg font-bold text-surface-800">Quality Experience</h3>
                <p class="mt-2 text-sm text-surface-500">Top-notch facilities, well-maintained courts, and programs designed for real improvement.</p>
            </div>
            <div class="bg-white rounded-2xl border border-surface-100 p-8 text-center">
                <div class="w-14 h-14 rounded-2xl bg-primary-100 flex items-center justify-center mx-auto mb-4">
                    <svg class="w-7 h-7 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
                </div>
                <h3 class="text-lg font-bold text-surface-800">Player Growth</h3>
                <p class="mt-2 text-sm text-surface-500">Programs and coaching tailored to help every player reach their next level.</p>
            </div>
        </div>
    </div>
</section>

<!-- Locations Summary -->
<?php if (!empty($facilities)): ?>
<section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-2xl sm:text-3xl font-extrabold text-surface-900 mb-4">Our Locations</h2>
        <p class="text-surface-500 mb-10">We operate <?= count($facilities) ?> location<?= count($facilities) > 1 ? 's' : '' ?> to serve our community.</p>
        <div class="flex flex-wrap justify-center gap-4">
            <?php foreach ($facilities as $f): ?>
            <a href="/facilities/<?= htmlspecialchars($f['slug']) ?>" class="px-6 py-3 bg-surface-50 hover:bg-primary-50 border border-surface-100 hover:border-primary-200 rounded-xl text-sm font-semibold text-surface-700 hover:text-primary-600 transition-colors">
                <?= htmlspecialchars($f['name']) ?>
                <?php if (!empty($f['city'])): ?><span class="text-surface-400"> · <?= htmlspecialchars($f['city']) ?></span><?php endif; ?>
            </a>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- CTA -->
<section class="py-20 bg-surface-50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-2xl sm:text-3xl font-extrabold text-surface-900">Join Our Community</h2>
        <p class="mt-4 text-lg text-surface-500">Ready to get started? Create your account and book your first session today.</p>
        <div class="mt-8 flex flex-wrap justify-center gap-4">
            <a href="/register" class="px-8 py-4 text-base font-bold text-white bg-primary-500 hover:bg-primary-600 rounded-2xl shadow-xl shadow-primary-500/25 transition-all">Get Started</a>
            <a href="/contact" class="px-8 py-4 text-base font-bold text-surface-700 bg-white border border-surface-200 hover:border-surface-300 rounded-2xl transition-all">Contact Us</a>
        </div>
    </div>
</section>
