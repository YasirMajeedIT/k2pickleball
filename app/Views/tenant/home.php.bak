<?php
/**
 * Tenant Home Page
 * Premium hero section, upcoming sessions, facility highlights, social proof.
 */
$orgName = htmlspecialchars($org['name'] ?? 'Sports Club');
$tagline = htmlspecialchars($branding['tagline'] ?? 'Book. Play. Compete.');
$heroImage = $branding['hero_image'] ?? null;
?>

<!-- ═══ HERO SECTION ═══ -->
<section class="relative overflow-hidden bg-gradient-to-br from-surface-900 via-surface-800 to-surface-900 text-white">
    <?php if ($heroImage): ?>
    <div class="absolute inset-0">
        <img src="<?= htmlspecialchars($heroImage) ?>" alt="" class="w-full h-full object-cover opacity-30">
        <div class="absolute inset-0 bg-gradient-to-r from-surface-900/90 via-surface-900/60 to-transparent"></div>
    </div>
    <?php else: ?>
    <div class="absolute inset-0 bg-gradient-to-br from-primary-900/30 via-transparent to-primary-900/20"></div>
    <?php endif; ?>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 lg:py-36">
        <div class="max-w-2xl">
            <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-primary-500/10 border border-primary-500/20 text-primary-300 text-sm font-medium mb-6">
                <span class="w-2 h-2 rounded-full bg-primary-400 animate-pulse"></span>
                Now Booking
            </div>
            <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold leading-tight">
                Welcome to<br>
                <span class="gradient-text"><?= $orgName ?></span>
            </h1>
            <p class="mt-6 text-lg sm:text-xl text-surface-300 leading-relaxed max-w-lg"><?= $tagline ?></p>
            <div class="mt-10 flex flex-wrap gap-4">
                <a href="/schedule" class="inline-flex items-center gap-2 px-8 py-4 text-base font-bold text-white bg-primary-500 hover:bg-primary-600 rounded-2xl shadow-xl shadow-primary-500/25 hover:shadow-primary-500/40 transition-all">
                    View Schedule
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
                <a href="/sessions" class="inline-flex items-center gap-2 px-8 py-4 text-base font-bold text-white border-2 border-white/20 hover:border-white/40 hover:bg-white/5 rounded-2xl transition-all">
                    Browse Programs
                </a>
            </div>
        </div>
    </div>

    <!-- Decorative elements -->
    <div class="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-white to-transparent"></div>
</section>

<!-- ═══ UPCOMING SESSIONS ═══ -->
<section class="py-20 bg-white" x-data="upcomingSessions()" x-init="load()">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl sm:text-4xl font-extrabold text-surface-900">Upcoming Sessions</h2>
            <p class="mt-3 text-lg text-surface-500 max-w-2xl mx-auto">Browse what's coming up and reserve your spot.</p>
        </div>

        <!-- Category Filter -->
        <div class="flex flex-wrap justify-center gap-2 mb-10" x-show="categories.length > 0" x-cloak>
            <button @click="filterCategory = null; load()"
                    class="px-4 py-2 rounded-full text-sm font-semibold transition-colors"
                    :class="!filterCategory ? 'bg-primary-500 text-white' : 'bg-surface-100 text-surface-600 hover:bg-surface-200'">
                All
            </button>
            <template x-for="cat in categories" :key="cat.id">
                <button @click="filterCategory = cat.id; load()"
                        class="px-4 py-2 rounded-full text-sm font-semibold transition-colors"
                        :class="filterCategory === cat.id ? 'text-white' : 'bg-surface-100 text-surface-600 hover:bg-surface-200'"
                        :style="filterCategory === cat.id ? 'background:' + (cat.color || '#6366f1') : ''"
                        x-text="cat.name">
                </button>
            </template>
        </div>

        <!-- Loading -->
        <div x-show="loading" class="text-center py-12">
            <div class="inline-flex items-center gap-3 text-surface-400">
                <svg class="animate-spin w-5 h-5" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/></svg>
                Loading schedule...
            </div>
        </div>

        <!-- Session Cards -->
        <div x-show="!loading && sessions.length > 0" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <template x-for="s in sessions.slice(0, 9)" :key="s.id">
                <a :href="'/schedule/' + s.id" class="group block bg-white rounded-2xl border border-surface-100 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 overflow-hidden">
                    <!-- Card header with category color -->
                    <div class="h-2 w-full" :style="'background:' + (s.category_color || '#6366f1')"></div>
                    <div class="p-5">
                        <!-- Date/Time -->
                        <div class="flex items-center gap-2 text-xs text-surface-400 mb-2">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
                            <span x-text="formatDate(s.start_time)"></span>
                            <span>·</span>
                            <span x-text="formatTime(s.start_time) + ' - ' + formatTime(s.end_time)"></span>
                        </div>
                        <!-- Title -->
                        <h3 class="text-lg font-bold text-surface-900 group-hover:text-primary-600 transition-colors" x-text="s.session_type_name || s.session_name || 'Session'"></h3>
                        <!-- Category badge -->
                        <div class="flex items-center gap-2 mt-2">
                            <span class="text-xs px-2.5 py-0.5 rounded-full font-medium text-white" :style="'background:' + (s.category_color || '#6366f1')" x-text="s.category_name || 'General'"></span>
                            <span x-show="s.skill_levels" class="text-xs px-2.5 py-0.5 rounded-full bg-surface-100 text-surface-600 font-medium" x-text="s.skill_levels"></span>
                        </div>
                        <!-- Bottom: Price + Availability -->
                        <div class="flex items-end justify-between mt-4 pt-4 border-t border-surface-50">
                            <div>
                                <div x-show="s.price > 0" class="text-2xl font-extrabold text-surface-900" x-text="'$' + parseFloat(s.price).toFixed(2)"></div>
                                <div x-show="!s.price || s.price == 0" class="text-2xl font-extrabold text-primary-500">Free</div>
                            </div>
                            <div class="text-right">
                                <div class="text-sm font-bold" :class="s.is_full ? 'text-red-500' : 'text-primary-500'" x-text="s.is_full ? 'Full' : s.spots_left + ' spots left'"></div>
                                <div class="text-[10px] text-surface-400" x-text="s.booked_count + ' registered'"></div>
                            </div>
                        </div>
                    </div>
                </a>
            </template>
        </div>

        <!-- Empty state -->
        <div x-show="!loading && sessions.length === 0" class="text-center py-16">
            <div class="w-16 h-16 rounded-full bg-surface-100 flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8 text-surface-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
            </div>
            <p class="text-lg font-semibold text-surface-600">No upcoming sessions</p>
            <p class="text-sm text-surface-400 mt-1">Check back soon for new sessions and classes.</p>
        </div>

        <!-- View All link -->
        <div x-show="sessions.length > 9" class="text-center mt-10">
            <a href="/schedule" class="inline-flex items-center gap-2 px-6 py-3 text-sm font-bold text-primary-600 bg-primary-50 hover:bg-primary-100 rounded-xl transition-colors">
                View Full Schedule →
            </a>
        </div>
    </div>
</section>

<!-- ═══ FACILITIES / LOCATIONS ═══ -->
<?php if (count($facilities) > 0): ?>
<section class="py-20 bg-surface-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl sm:text-4xl font-extrabold text-surface-900">Our Locations</h2>
            <p class="mt-3 text-lg text-surface-500">Find a facility near you and start playing.</p>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-<?= min(count($facilities), 3) ?> gap-8">
            <?php foreach (array_slice($facilities, 0, 6) as $f): ?>
            <a href="/facilities/<?= htmlspecialchars($f['slug']) ?>" class="group block bg-white rounded-2xl shadow-sm border border-surface-100 overflow-hidden hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
                <div class="h-48 bg-gradient-to-br from-primary-100 to-primary-50 flex items-center justify-center">
                    <svg class="w-16 h-16 text-primary-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                </div>
                <div class="p-6">
                    <h3 class="text-xl font-bold text-surface-900 group-hover:text-primary-600 transition-colors"><?= htmlspecialchars($f['name']) ?></h3>
                    <?php if (!empty($f['city'])): ?>
                    <p class="mt-1 text-sm text-surface-400"><?= htmlspecialchars($f['city'] . (!empty($f['state']) ? ', ' . $f['state'] : '')) ?></p>
                    <?php endif; ?>
                    <div class="mt-4 flex items-center gap-2 text-primary-600 text-sm font-semibold group-hover:gap-3 transition-all">
                        View Location
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                    </div>
                </div>
            </a>
            <?php endforeach; ?>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- ═══ CTA SECTION ═══ -->
<section class="py-20 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl sm:text-4xl font-extrabold text-surface-900">Ready to Play?</h2>
        <p class="mt-4 text-lg text-surface-500 max-w-2xl mx-auto">Create your free account to book sessions, track your progress, and join our community.</p>
        <div class="mt-10 flex flex-wrap justify-center gap-4">
            <a href="/register" class="px-8 py-4 text-base font-bold text-white bg-primary-500 hover:bg-primary-600 rounded-2xl shadow-xl shadow-primary-500/25 transition-all">
                Create Free Account
            </a>
            <a href="/schedule" class="px-8 py-4 text-base font-bold text-surface-700 bg-surface-100 hover:bg-surface-200 rounded-2xl transition-all">
                Browse Schedule
            </a>
        </div>
    </div>
</section>

<script>
function upcomingSessions() {
    return {
        sessions: [],
        categories: [],
        loading: true,
        filterCategory: null,

        async load() {
            this.loading = true;
            try {
                // Load categories
                if (!this.categories.length) {
                    const catRes = await fetch(baseApi + '/api/public/categories');
                    const catJson = await catRes.json();
                    this.categories = catJson.data || [];
                }

                // Build schedule URL
                let url = baseApi + '/api/public/schedule?start=' + this.today() + '&end=' + this.futureDate(14);
                const fac = window.tenantApp?.selectedFacility || JSON.parse(localStorage.getItem('selected_facility') || 'null');
                if (fac?.id) url += '&facility_id=' + fac.id;
                else if (ORG.facilities.length) url += '&facility_id=' + ORG.facilities[0].id;
                if (this.filterCategory) url += '&category_id=' + this.filterCategory;

                const res = await fetch(url);
                const json = await res.json();
                this.sessions = json.data || [];
            } catch(e) {
                this.sessions = [];
            }
            this.loading = false;
        },

        today() { return new Date().toISOString().split('T')[0]; },
        futureDate(days) {
            const d = new Date();
            d.setDate(d.getDate() + days);
            return d.toISOString().split('T')[0];
        },
        formatDate(dt) {
            if (!dt) return '';
            const d = new Date(dt);
            return d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
        },
        formatTime(dt) {
            if (!dt) return '';
            const d = new Date(dt);
            return d.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit', hour12: true });
        },
    };
}

// Listen for facility changes
window.addEventListener('facility-changed', () => {
    document.querySelectorAll('[x-data]').forEach(el => {
        if (el.__x?.$data?.load) el.__x.$data.load();
    });
});
</script>
